import React, { useState } from 'react';

const Enroll = () => {
    const [modal, setModal] = useState(null);
    return (
        <div>
            Enroll
        </div>
    );
};

export default Enroll;