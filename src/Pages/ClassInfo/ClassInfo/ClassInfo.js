import React from 'react';
import { useParams } from 'react-router-dom';

const ClassInfo = () => {
    const { id } = useParams();
    return (
        <div>
            
        </div>
    );
};

export default ClassInfo;