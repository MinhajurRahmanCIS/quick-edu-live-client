import React from 'react';
import { useParams } from 'react-router-dom';
const ClassWork = () => {
    const { id } = useParams();
    return (
        <div>
            
        </div>
    );
};

export default ClassWork;