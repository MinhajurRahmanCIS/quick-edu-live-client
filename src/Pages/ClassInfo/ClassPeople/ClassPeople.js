import React from 'react';
import { useParams } from 'react-router-dom';
const ClassPeople = () => {
    const { id } = useParams();
    return (
        <div>
            
        </div>
    );
};

export default ClassPeople;