import React from 'react';
import { useParams } from 'react-router-dom';
const ClassPeople = () => {
    const { id } = useParams();
    return (
        <div>
            Under Development
        </div>
    );
};

export default ClassPeople;